<UserControl
    x:Class="PanoramioClient.ImagePushpin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PanoramioClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    DataContext="{Binding Source={StaticResource Locator}, Path=PushpinViewModel}"
    Loaded="ImagePushpin_OnLoaded">
    <Border Canvas.ZIndex="1" Width="300" Height="300"  Background="WhiteSmoke" BorderBrush="Black" BorderThickness="5">
        <FlipView Canvas.ZIndex="0"  ItemsSource="{Binding Path=ThumbnailsImages}">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding}" Stretch="UniformToFill" />
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
        </Border>
</UserControl>