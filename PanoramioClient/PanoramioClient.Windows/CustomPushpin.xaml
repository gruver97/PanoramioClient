<UserControl
    x:Class="PanoramioClient.CustomPushpin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PanoramioClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    DataContext="{Binding Source={StaticResource Locator}, Path=PushpinViewModel}">
    
    <GridView Background="Black" ItemsSource="{Binding Path=ThumbnailsImages}" Width="300" Height="200">
        <GridView.ItemTemplate>
            <DataTemplate>
                <Image Source="{Binding}" Stretch="UniformToFill" Width="30" Height="30"></Image>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</UserControl>
